import React from 'react';

function Brother(props) {
    // const sample = props.delivered;
    return (
        <div>
            <h2>{props.delivered}</h2>
        </div>
    );
};

export default Brother;